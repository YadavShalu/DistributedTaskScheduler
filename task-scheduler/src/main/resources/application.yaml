spring:
  application:
    name: task-scheduler
  datasource:
    url: jdbc:postgresql://localhost:5432/scheduler
    username: scheduler
    password: scheduler
  jpa:
    hibernate: 
      ddl-auto: update  
    show-sql: true
  
redisson:
  address: redis://localhost:6379


server:
  port: 8080
